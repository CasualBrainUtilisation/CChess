#ifndef CCMoves
#define CCMoves

#include "CCBoard.h"

// The different types of chess moves, to be easily defined in an enum
enum MoveType
{
};

// The struct for a move itself
typedef struct Move
{
} Move;

#endif